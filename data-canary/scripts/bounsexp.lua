-- local bounsExp = Action()

-- function bounsExp.onUse(player, item, fromPosition, target, toPosition, isHotkey)

    -- if player:getStorageValue(1234) <= 0 then
     -- player:setStorageValue(1234, 1)
    -- player:removeItem(34005,1)
    -- player:sendTextMessage(MESSAGE_EVENT_ADVANCE, 'You have received 10% Bouns Exp!')
    -- else
        -- player:sendTextMessage(MESSAGE_EVENT_ADVANCE, 'you aleardy use this item!')
    -- end
    -- return true
-- end

-- bounsExp:id(34005) -- the item is a scythe
-- bounsExp:register()