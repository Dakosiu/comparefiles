-- local rings = {
                -- [2123] = {
				           -- checkTime = 3, -- in seconds
						   -- effect = CONST_ME_MAGIC_GREEN,
				           -- ["Required Items"] = {
						                          -- { itemid = 1198, count = 3 },
												  -- { itemid = 2160, count = 1 },
												-- },
						 -- }
			  -- }
			  
-- local players = {}
			  

-- local creatureevent = CreatureEvent("onEquiped_Ring")
-- function creatureevent.onThink(player)
   -- if player:getName() == "Dakoseq" then
      
   -- local slot = player:getSlotItem(CONST_SLOT_RING)
	  
	-- if slot then
	     -- if not players[player:getId()].active then
		    -- players[player:getId()].active = false
	     -- end
		 
	     -- local v = rings[slot:getId()]
		 -- if v then
		   -- addEvent(function()
		      -- local user = Player(player:getId())
		
		      -- if not user then
		         -- return false
		      -- end
			  
			  -- user:say("kuklux clan")
           -- end, 3000)
		 -- end
	-- end
	  
   -- end
   -- return true
-- end
-- creatureevent:register()

-- creatureevent = CreatureEvent("onLogin_Ring")
-- function creatureevent.onLogin(player)
	-- player:registerEvent("onEquiped_Ring")
	-- return true
-- end
-- creatureevent:register()		
   


